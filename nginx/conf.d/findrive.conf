upstream django {
    server web:8000;
}

# HTTP server
server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge (for future SSL setup)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Static files
    location /static/ {
        alias /static/;
        expires 1h;
        add_header Cache-Control "public";
    }

    # Media files
    location /media/ {
        alias /media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Django application
    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_redirect off;
        proxy_read_timeout 120s;
        proxy_connect_timeout 120s;
        
        # Important for session cookies
        proxy_cookie_path / /;
    }
}

# HTTPS server (uncomment after SSL certificate is generated)
# To enable HTTPS:
# 1. Generate certificate: ./init-letsencrypt.sh
# 2. Update domain name in ssl_certificate paths below
# 3. Uncomment this block
# 4. Restart nginx: docker-compose restart nginx
#
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name _;
#
#     # SSL certificates (update domain name after generation)
#     ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     # Static files
#     location /static/ {
#         alias /static/;
#         expires 1h;
#         add_header Cache-Control "public";
#     }
#
#     # Media files
#     location /media/ {
#         alias /media/;
#         expires 7d;
#         add_header Cache-Control "public";
#     }
#
#     # Django application
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Port $server_port;
#         proxy_redirect off;
#         proxy_read_timeout 120s;
#         proxy_connect_timeout 120s;
#         proxy_cookie_path / /;
#     }
# }

